# Generated by stage-hosttools-atlas.sh with absolute paths resolved for THIS machine
# ($HOSTTOOLS, default ~/hosttools). Committing it forces one developer's home directory
# onto everyone else, so it is generated locally and never tracked.
hosttools-env.sh
